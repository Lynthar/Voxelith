/target


# Workspace-local agent settings. The modeling skill under .claude/skills/
# is deliberately tracked: it ships with the tool, so a fresh clone has it.
.claude/*
!.claude/skills/
CLAUDE.md
# Local design records, same standing decision as CLAUDE.md: a document
# in the tracked tree silently drifts from the code it describes.
ARCHITECTURE_CONVERGENCE.md
/docs/design/
# Eval run artifacts: the cases are the asset, a given run is not.
/evals/runs
